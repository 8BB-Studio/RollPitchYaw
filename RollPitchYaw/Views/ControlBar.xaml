<UserControl x:Class="RollPitchYaw.Views.ControlBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RollPitchYaw.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="LightGray" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontSize" Value="12" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontSize" Value="12" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Roll:" />
            <TextBlock Grid.Column="1" Text="{Binding Roll}" />
            <Slider Grid.Column="2"
                    Maximum="360"
                    Minimum="0"
                    Value="{Binding Roll}" />
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Pitch:" />
            <TextBlock Grid.Column="1" Text="{Binding Pitch}" />
            <Slider Grid.Column="2"
                    Maximum="90"
                    Minimum="-90"
                    Value="{Binding Pitch}" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Yaw:" />
            <TextBlock Grid.Column="1" Text="{Binding Yaw}" />
            <Slider Grid.Column="2"
                    Maximum="90"
                    Minimum="-90"
                    Value="{Binding Yaw}" />
        </Grid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Thickness:" />
            <TextBlock Grid.Column="1" Text="{Binding Thickness}" />
            <Slider Grid.Column="2"
                    Maximum="0.5"
                    Minimum="0.01"
                    Value="{Binding Thickness}" />
        </Grid>

        <UniformGrid Grid.Row="4" Columns="6">
            <Button Background="Lime"
                    Command="{Binding SpindleCommand}"
                    CommandParameter="START"
                    Content="START" />
            <Button Background="Red"
                    Command="{Binding SpindleCommand}"
                    CommandParameter="STOP"
                    Content="STOP" />
        </UniformGrid>

    </Grid>

</UserControl>
